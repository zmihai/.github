name: Reusable Security Scan

on:
  workflow_call:
    inputs:
      scan-dependencies:
        description: 'Scan npm dependencies for vulnerabilities'
        required: false
        type: boolean
        default: true
      scan-code:
        description: 'Run CodeQL analysis'
        required: false
        type: boolean
        default: false
      language:
        description: 'Language for CodeQL (javascript, python, etc.)'
        required: false
        type: string
        default: 'javascript'
      working-directory:
        description: 'Working directory for commands'
        required: false
        type: string
        default: '.'

defaults:
  run:
    shell: bash

jobs:
  dependency-scan-javascript:
    if: ${{ inputs.scan-dependencies && inputs.language == 'javascript' }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        working-directory: ${{ inputs.working-directory }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node Environment
        uses: zmihai/.github/actions/setup-node-env@v0.4.0
        with:
          node-version: 'lts/*'
          working-directory: ${{ inputs.working-directory }}
          install-dependencies: 'false'

      - name: Run npm audit
        run: npm audit --audit-level=moderate
        continue-on-error: true

  dependency-scan-python:
    if: ${{ inputs.scan-dependencies && inputs.language == 'python' }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        working-directory: ${{ inputs.working-directory }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python Environment
        uses: zmihai/.github/actions/setup-python-env@v0.4.0
        with:
          python-version: '3.11'
          working-directory: ${{ inputs.working-directory }}
          install-dependencies: 'false'

      - name: Run pip audit
        run: pip install pip-audit && pip-audit
        continue-on-error: true

  codeql-scan:
    if: ${{ inputs.scan-code }}
    runs-on: ubuntu-latest
    timeout-minutes: 30
    defaults:
      run:
        working-directory: ${{ inputs.working-directory }}
    permissions:
      security-events: write
      contents: read
      actions: read
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v4
        with:
          languages: ${{ inputs.language }}

      - name: Autobuild
        uses: github/codeql-action/autobuild@v4

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v4
