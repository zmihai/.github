name: CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  ci:
    uses: zmihai/.github/.github/workflows/reusable-ci.yml@main
    with:
      node-version: '20'
      run-lint: true
      run-test: true
      run-build: true
