name: CI

on:
  push:
    branches: [$default-branch, develop]
  pull_request:
    branches: [$default-branch, develop]

jobs:
  ci:
    uses: zmihai/.github/.github/workflows/reusable-ci.yml@7a5418fc2bd4181a7c08057b0a1570a225ce12d8 # ratchet:zmihai/.github/.github/workflows/reusable-ci.yml@v0.6.6
    with:
      language: 'javascript'
      language-version: '20'
      run-lint: true
      run-test: true
      run-build: true
      build-before-test: false
